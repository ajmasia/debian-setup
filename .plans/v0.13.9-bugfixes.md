# Release Plan: v0.13.9 — Bug Fixes & Robustness

Current version: `0.13.8` → Target: `0.13.9`

## Tasks

Each task is a single commit on `develop`. Validate after each step before proceeding.

---

### Task 1: Fix label collision in appthemes.sh

- [x] Rename appthemes-local labels to `_APPTHEMES_ATUIN_LABEL`, `_APPTHEMES_STARSHIP_LABEL`, `_APPTHEMES_ALACRITTY_LABEL` (and any other collisions)
- [x] Update all references in `_APPTHEMES_TASKS` array
- [x] Validate: Shell Tools menu shows "Atuin", "Starship" correctly
- [x] Validate: Software > Terminals shows "Alacritty" correctly
- [x] Validate: UI and Theming > App Themes shows theme labels correctly
- [x] Commit: `fix: rename appthemes labels to avoid collision with module globals`

**Bug**: `gnome/appthemes.sh` defines `_ATUIN_LABEL`, `_STARSHIP_LABEL`, `_ALACRITTY_LABEL` which overwrite the globals from `shell/atuin.sh`, `shell/starship.sh`, and `software/alacritty.sh`. The Shell Tools and Terminals menus show "Atuin Theme" instead of "Configure Atuin", etc.

---

### Task 2: Fix memory percentage in health.sh

- [x] Replace `free -h` with `free --mebi` for awk arithmetic
- [x] Format the display value separately for human-readable output (MiB → GiB)
- [x] Validate: Health check shows realistic memory percentage
- [x] Commit: `fix: use numeric free output for memory percentage in health check`

**Bug**: `free -h` outputs human-readable strings (e.g., "7.6Gi"). Awk attempts arithmetic division on these strings → always shows `(0.0%)`.

---

### Task 3: Fix browserthemes.sh single-iteration loop

- [x] Remove unnecessary `while true`/`done` wrapper (informational-only module, no loop needed)
- [x] Validate: UI and Theming > Browser Themes shows guide and returns correctly
- [x] Commit: `fix: remove unnecessary while loop in browserthemes wizard`

**Bug**: `while true` loop has an unconditional `return` on first iteration — the wizard never loops back after showing info.

---

### Task 4: Fix spinner leak on Ctrl+C

- [x] Update INT trap to call `ui::spin_stop` before exiting
- [x] Ensure `ui::spin_stop` is safe to call when no spinner is running
- [x] Validate: Ctrl+C during spinner leaves no orphaned processes
- [x] Commit: `fix: stop spinner on SIGINT to prevent orphaned subshell`

**Bug**: The INT trap in the entry point does not call `ui::spin_stop`. If Ctrl+C is pressed while a spinner is running, the background subshell is orphaned.

---

### Task 5: Reject unknown CLI flags

- [x] Add `*)` case that prints error and shows help, then exits with code 1
- [x] Validate: `debian-setup --foo` prints error and exits
- [x] Validate: Valid flags still work normally
- [x] Commit: `fix: reject unknown CLI flags with error message`

**Issue**: Unknown flags like `debian-setup --foo` are silently ignored and fall through to the normal menu.

---

### Task 6: Guard read calls against set -e

- [x] Add `|| true` to `read -r response` in `gum.sh`
- [x] Add `|| true` to `read -rs -d'R'` in `ui.sh`
- [x] Validate: Script still works normally after changes
- [x] Commit: `fix: guard read calls against set -e in gum and ui libs`

**Issue**: `read` calls can fail under `set -e` if stdin/terminal is unavailable.

---

### Task 7: Fix nerdfonts OTF detection + breadcrumb path

- [x] Update check to include both `*.ttf` and `*.otf` files
- [x] Validate: OTF-only fonts detected correctly after install
- [x] Fix breadcrumb: nav shows "Software > Fonts" but module is under "UI and Theming > Fonts"
- [x] Validate: Breadcrumb shows correct path
- [x] Commit: `fix: correct nerdfonts breadcrumb path to UI and Theming`

**Issue**: `nerdfonts.sh` only checks for `*.ttf` files. OTF-only fonts report as not installed.

---

### Task 8: Fix APT label Title Case

- [x] Change `_APT_LABEL` to `"Configure APT Sources"`
- [x] Validate: Package Managers menu shows "APT Sources"
- [x] Commit: `fix: capitalize APT Sources label to match Title Case convention`

**Issue**: `_APT_LABEL="Configure APT sources"` — "sources" should be "Sources".

---

### Task 9: Remove dead vscode.txt package list

- [x] Remove `packages/deb/vscode.txt`
- [x] Validate: VS Code install/remove still works
- [x] Commit: `chore: remove unused vscode.txt package list`

**Issue**: `packages/deb/vscode.txt` is never read. The URL is hardcoded in `vscode.sh`.

---

## Improvements

---

### Task 10: Switch Claude Code to native installer

- [x] Replace npm-based install with native installer: `curl -fsSL https://claude.ai/install.sh | bash`
- [x] Update check: `command -v claude` (already in place)
- [x] Update remove: `rm -f ~/.local/bin/claude && rm -rf ~/.local/share/claude`
- [x] Remove Node.js dependency requirement (npm install is deprecated)
- [x] Update label/description to reflect native install
- [x] Validate: Install/remove/update cycle works correctly
- [x] Commit: `feat: switch Claude Code to native installer`

**Improvement**: The npm install method (`npm install -g @anthropic-ai/claude-code`) is deprecated. The native installer is faster, has no dependencies, and auto-updates. Reference: https://code.claude.com/docs/en/setup

---

### Task 11: Reorder main menu — System Essentials first

- [x] Move "System Essentials" before "Hardware Support" in `menu::main()` item list
- [x] Update sourcing order in entry point if needed (not needed — sourcing is independent of menu order)
- [x] Validate: Main menu shows System Essentials as first option
- [x] Commit: `refactor: move System Essentials to first position in main menu`

**Improvement**: System Essentials (sudoers, editor, zram, etc.) is the most common first step after install. It should be the first menu option.

---

### Task 12: Reorder System Essentials — Kernel in third position

- [x] Move Kernel entry to third position in `_SYSTEM_TASKS` array (after Sudoers, Password Feedback)
- [x] Validate: System Essentials submenu shows Kernel in third place
- [x] Commit: `refactor: move Kernel to third position in System Essentials`

**Improvement**: Kernel configuration is a common early task. Place it after Sudoers and Password Feedback for a more logical flow.

---

### Task 13: Promote Git to top-level menu after OpenSSH

- [x] Extract Git from Development > Tools into its own top-level module
- [x] Reused existing `development/git.sh` — added `_GIT_TASKS` array for search
- [x] Remove Git entry from `_DEVTOOLS_TASKS`
- [x] Add Git entry to `menu::main()` after OpenSSH Server
- [x] Register `_GIT_TASKS` in `_SEARCH_ARRAYS`
- [x] Update breadcrumb from "Development > Tools > Git" to "Git"
- [x] Validate: Main menu shows Git after OpenSSH Server
- [x] Validate: Development > Tools no longer shows Git
- [x] Validate: Search modes still find Git
- [x] Commit: `refactor: promote Git to top-level menu after OpenSSH`

**Improvement**: Git configuration is fundamental enough to deserve its own top-level menu entry, not buried under Development > Tools.

---

### Task 14: Add Node.js memory limit option to Node module

- [x] Add `_node::memory_configured()` and `_node::memory_current()` to detect existing config
- [x] Add `_node::memory_apply()` wizard with RAM-aware options (4/8/16+ GB detection)
- [x] Write to `~/.bashrc` with explanatory comment and marker
- [x] Add remove option to clean the entry from `~/.bashrc`
- [x] Integrate into node wizard (new "Configure memory limit" option when session_ready)
- [x] Validate: Option appears in Node.js wizard, writes correctly, detects existing config
- [x] Commit: `feat: add Node.js memory limit configuration to Node module`

**Improvement**: Large projects and monorepos often hit Node.js default heap limits. Offer a guided option to set `--max-old-space-size` in `~/.bashrc` with RAM-aware defaults.

---

### Task 15: Add Spotify client to Software > Media

- [x] Create `src/modules/software/spotify.sh` following Flatpak leaf module pattern
- [x] Flatpak app ID: `com.spotify.Client`
- [x] Implement `spotify::check()` — verify app installed via flatpak list
- [x] Implement `spotify::status()` — report if not installed
- [x] Implement `spotify::apply()` — wizard with install/remove via `flatpak install/uninstall`
- [x] Check `command -v flatpak` before install (clear error if missing)
- [x] Add entry to `_SOFTWARE_TASKS` after Media
- [x] Source file in entry point
- [x] Validate: Software shows Spotify option
- [x] Validate: Install/remove cycle works
- [x] Commit: `feat: add Spotify client module to Software > Media`

**Improvement**: Spotify is a common media app. Install via Flatpak (`com.spotify.Client`) from Flathub.

---

### Task 16: Move Kernel from System Essentials to Hardware Support

- [x] Remove Kernel entry from `_SYSTEM_TASKS` array in `system/main.sh`
- [x] Add Kernel entry in first position of `_HARDWARE_TASKS` array in `hardware/main.sh`
- [x] Update breadcrumb in `kernel.sh` to "Hardware Support > Backports kernel"
- [x] Validate: Hardware Support shows Kernel as first option
- [x] Validate: System Essentials no longer shows Kernel
- [x] Commit: `refactor: move Kernel from System Essentials to Hardware Support`

**Improvement**: Kernel configuration (drivers, boot parameters) is hardware-related, not system-essential.

---

### Task 17: Reorder main menu for new machine setup flow

- [x] Update `menu::main()` items order: System Essentials, Package Managers, Hardware Support, OpenSSH Server, Git, Dotfiles, Shell Tools, Development, UI and Theming, Software, Virtualization, Health
- [x] Validate: Main menu follows the new logical order
- [x] Commit: `refactor: reorder main menu for logical new machine setup flow`

**Improvement**: Order follows natural setup flow: foundation → access → tools → customization → apps → check.

---

### Task 18: Fix QEMU default network enable

- [x] Investigate: `_qemu::default_net_active()` uses `virsh` without sudo (may fail if session not active)
- [x] Fix: use `sudo virsh` in check functions or ensure consistency between check and enable
- [x] Fix: show actual error when `virsh net-start` or `virsh net-autostart` fail (currently swallowed by `|| true`)
- [x] Validate: "Enable default network" succeeds and status updates correctly
- [x] Commit: `fix: use sudo for virsh network checks and show errors on failure`

**Bug**: The check `_qemu::default_net_active()` uses `virsh` without sudo but `_qemu::enable_network()` uses `sudo virsh`. If `virsh net-start` fails, the error is silently swallowed and the wizard returns to menu with no feedback.

---

### Task 19: Update GNOME extensions list

- [x] Add `caffeine@patapon.info|Caffeine` to `packages/gnome/extensions.txt`
- [x] Add `gnome-shell-extension-prefs` and `gnome-shell-extension-manager` to GNOME extensions APT packages
- [x] Validate: GNOME Extensions wizard shows Caffeine and installs Extensions/Extensions Manager
- [x] Commit: `feat: add Caffeine extension and Extensions Manager to GNOME setup`

**Change**: Add Caffeine (prevent screen suspend), Extensions app and Extensions Manager (manage extensions from desktop).

---

## Release

---

### Task 20: Release v0.13.9

- [ ] `chore: bump version to 0.13.9` — update `VERSION`
- [ ] `docs: update README and CHANGELOG for v0.13.9` — update `CHANGELOG.md` and `README.md` version badge
- [ ] Merge to `main`: `git checkout main && git merge develop`
- [ ] Push both: `git push origin develop main`
