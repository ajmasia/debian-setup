# Release Plan: v1.0.0

Current version: `0.13.10` → Target: `1.0.0`

## Tasks

Each task is a single commit on `develop`. Commit first, then validate. If validation fails, rollback with `git checkout`.

---

### Task 1: Refactor search functions in menu.sh (DRY)

**Why**: `menu::search`, `menu::search_to_install`, `menu::search_to_remove` share ~80% identical code. Extracting helpers reduces duplication and provides reusable building blocks for `--option` and `--list`.

**Files:** `src/menu.sh`

- [x] Add helper `menu::_collect_leaf_tasks` that populates `_COLLECTED_LABELS` and `_COLLECTED_APPLY_FNS` arrays
  - Accepts optional filter argument: `""` (all), `installed`, `available`
  - Iterates `_SEARCH_ARRAYS`, skips `::run` entries, strips "Configure " prefix
  - Sets `_COLLECTED_TOTAL` with total count
- [x] Add helper `menu::_run_choice` that routes a selected label to its apply_fn
- [x] Rewrite `menu::search` using helpers
- [x] Rewrite `menu::search_to_install` using helpers
- [x] Rewrite `menu::search_to_remove` using helpers
- [x] Commit: `refactor: extract shared helpers from search functions in menu`
- [x] Validate: `debian-setup -s` → global search works
- [x] Validate: `debian-setup -si` → search-to-install works
- [x] Validate: `debian-setup -sr` → search-to-remove works
- [x] Validate: Back/Exit/Ctrl+C behavior unchanged

---

### Task 2: Add `--list` flag

**Why**: Lists all available tasks to stdout. Required by shell completions (Task 6) to dynamically provide `-o` values. Also useful for scripting.

**Files:** `debian-setup`, `src/menu.sh`

- [x] Add `menu::list` function in `src/menu.sh`
  - Calls `menu::_collect_leaf_tasks` (no filter)
  - Also includes main menu category names
  - Prints one item per line, sorted alphabetically
- [x] Add `-l, --list` flag to CLI parsing in `debian-setup`
  - Runs BEFORE header/gum::check (no UI, just stdout)
  - Sources all needed files, calls `menu::list`, exits
- [x] Add flag to `show_help` output
- [x] Commit: `feat: add --list flag to print all available tasks`
- [x] Validate: `debian-setup --list` prints all tasks (104 unique)
- [x] Validate: `debian-setup --list | wc -l` → 104
- [x] Validate: `debian-setup --list | grep -i docker` finds Docker
- [x] Validate: categories appear in list
- [x] Validate: no duplicates (`sort | uniq -d` → empty)

---

### Task 3: Add `-o, --option <name>` flag

**Why**: Jump directly to a specific task or category without navigating menus. Enables scripting and fast access.

**Files:** `debian-setup`, `src/menu.sh`

- [x] Add `menu::jump` function in `src/menu.sh`
  - Takes a query string argument
  - Collects all leaf tasks via `menu::_collect_leaf_tasks`
  - Also includes main menu categories with their `::run`/`::apply` functions
  - Case-insensitive substring matching
  - Exactly 1 match → call apply_fn/run_fn, then `ui::goodbye`
  - Multiple matches → show `gum::filter` with matches only
  - 0 matches → `log::error` + exit 1
- [x] Add `-o, --option` flag to CLI parsing in `debian-setup`
  - `shift` to get argument, error if missing
  - Store in `_jump_target` variable
- [x] Route: after header, if `_jump_target` set, call `menu::jump "$_jump_target"`Z
- [x] Add flag to `show_help`
- [x] Commit: `feat: add -o/--option flag for direct task access`
- [x] Validate: `debian-setup -o spotify` → Spotify wizard
- [x] Validate: `debian-setup -o docker` → Docker wizard
- [x] Validate: `debian-setup -o development` → Development submenu
- [x] Validate: `debian-setup -o nonexistent` → error + exit 1
- [x] Validate: `debian-setup -o br` → filter with Brave/Browsers matches
- [x] Validate: `debian-setup --option "node"` → Node.js wizard

---

### Task 4: Customizable alias names in Neovim module

**Why**: The Neovim module adds aliases to `.bash_aliases` using the config directory name (e.g., `alias lazyvim='...'`). The user should be able to choose the alias name instead of using the hardcoded config name.

**Files:** `src/modules/software/neovim.sh`

- [x] After "Add alias to .bash_aliases?" → Yes, prompt for alias name using `gum::input`
  - Default value: config_name (e.g., "lazyvim")
  - Placeholder: "Alias name"
  - Validate: non-empty, valid shell identifier (letters, digits, underscore, hyphen)
- [x] Update alias line to use the chosen name: `alias <chosen>='NVIM_APPNAME=${config_name} nvim'`
- [x] Update the comment to reflect the alias name
- [x] Update removal logic to handle custom alias names (search by `NVIM_APPNAME=${config_name}` pattern, not by alias name)
- [x] Commit: `feat: prompt for custom alias name in Neovim module`
- [x] Validate: LazyVim install → alias prompt shows default name
- [x] Validate: custom alias name → writes correct line to .bash_aliases
- [x] Validate: removal cleans up correctly regardless of alias name

---

### Task 5: Refactor Health into Settings submenu

**Why**: "Health" is ambiguous. "Settings" groups CLI management: simplified health, logs, completions, and about info. Highlighted in Overlay1 color to separate from regular modules.

**Files:** `src/modules/health.sh`, `src/modules/diagnostics/main.sh` → `src/modules/settings/main.sh`, `src/modules/diagnostics/logs.sh` → `src/modules/settings/logs.sh`, `src/modules/settings/completions.sh` (new), `src/modules/settings/about.sh` (new), `completions/debian-setup.bash` (new), `completions/debian-setup.zsh` (new), `src/menu.sh`, `debian-setup`

- [x] Simplify `health.sh`: replace 9 duplicated `_check_*_tasks()` with generic `health::_check_group` helper
  - System status (CPU, RAM, disk, uptime) — keep as is
  - Dependencies — keep as is
  - Task summary: one line per group with counter (e.g., "Development: 8/12")
- [x] Create `completions/debian-setup.bash`
  - `complete -F _debian_setup debian-setup`
  - All flags: `-v`, `--version`, `-h`, `--help`, `-s`, `--search`, `-si`, `--search-to-install`, `-sr`, `--search-to-remove`, `-l`, `--list`, `-o`, `--option`, `--update`, `--uninstall`
  - After `-o`/`--option`: dynamic via `debian-setup --list 2>/dev/null`
- [x] Create `completions/debian-setup.zsh`
  - `#compdef debian-setup` header, same flags, `compadd` for `-o`
- [x] Create `src/modules/settings/completions.sh` — install/uninstall completion symlinks
- [x] Create `src/modules/settings/about.sh` — version, install path, log dir
- [x] Rename `src/modules/diagnostics/` → `src/modules/settings/`
- [x] Rewrite `settings/main.sh` submenu: System Health, Logs, Completions, About, Back, Exit
- [x] Update `menu::main()` and `_MENU_CATEGORIES`: "Health" → "⚙ Settings"
- [x] Update `debian-setup` source paths
- [x] Commit: `refactor: rename Health to Settings with simplified health and completions`
- [x] Validate: main menu shows "⚙ Settings"
- [x] Validate: Settings > System Health shows machine status + group counters
- [x] Validate: Settings > Logs works
- [x] Validate: Settings > Completions installs/uninstalls symlinks
- [x] Validate: Settings > About shows version and paths
- [x] Validate: `debian-setup -o settings` works

---

### Task 6: Shell completions with cache-based autocomplete

**Why**: Tab completion for CLI flags and `-o` task names. Cache-based to avoid loading all modules on TAB.

**Files:** `completions/debian-setup.bash`, `completions/debian-setup.zsh`, `install.sh`, `src/menu.sh`, `src/modules/settings/completions.sh`

- [x] Switch completions from `debian-setup --list` to cache file (`~/.cache/debian-setup/tasks.txt`)
- [x] Update `menu::list()` to write cache on every call
- [x] Settings > Completions: separate install/remove for bash and zsh, with `(current shell)` suffix
- [x] Settings > Completions: generate cache on install (`menu::list > /dev/null`)
- [x] `install.sh`: `refresh_completions()` only refreshes existing symlinks, `remove_completions()` on uninstall
- [x] Commit: `feat: add bash and zsh shell completions with cache-based autocomplete`
- [x] Validate: `debian-setup -o <TAB>` shows task names
- [x] Validate: Settings > Completions > Install/Remove works for bash and zsh
- [x] Validate: `debian-setup <TAB>` shows flags

---

### Task 7: Add accent change and window buttons to Appearance modules

**Why**: Currently you can only choose the accent color at install time. Adding "Change accent" options allows reconfiguring without reinstalling. Window button layout (which buttons, left/right position) is a common GNOME customization.

**Files:** `src/modules/gnome/gtktheme.sh`, `src/modules/gnome/icons.sh`, `src/modules/gnome/cursors.sh`, `src/modules/gnome/appearance.sh`

- [x] **GTK Theme**: add "Change Accent" option (when installed)
  - Show accent chooser, reinstall theme with new accent
  - Preserve GTK4/libadwaita setup and terminal CSS
- [x] **Icons**: add "Change Folder Color" option (when installed)
  - Re-run `papirus-folders -C cat-mocha-<accent>` without reinstalling Papirus
- [x] **Cursors**: add "Change Variant" option (when installed)
  - Download and install new variant, remove previous
- [x] **Window Buttons**: new leaf module `windowbuttons.sh` in Appearance
  - Show current layout (e.g., `appmenu:close`)
  - Presets: "Right: close (default)", "Right: minimize, maximize, close", "Left: close, minimize, maximize"
  - Custom via `gum::input`
  - Uses `gsettings set org.gnome.desktop.wm.preferences button-layout`
- [x] Commit: `feat: add accent change and window button layout to Appearance`
- [x] Validate: GTK Theme > Change Accent works
- [x] Validate: Icons > Change Folder Color works
- [x] Validate: Cursors > Change Variant works
- [x] Validate: Window Buttons presets apply correctly

---

### Task 8: Update `.ai-context/` docs

**Files:** `.ai-context/architecture.md`, `.ai-context/modules.md`

- [x] Fix "34 `_*_TASKS` arrays" → 28 in architecture.md
- [x] Fix "11 top-level categories" → 12 in architecture.md
- [x] Add `menu::jump()`, `menu::list()`, `menu::_collect_leaf_tasks()` to menu system section
- [x] Add `-o`, `-l` flags to entry point description
- [x] Document completions, Settings, Appearance modules
- [x] Commit: `docs: update architecture and modules docs for v1.0.0`
- [x] Validate: docs match actual code

---

### Task 9: Release v1.0.0

- [x] `chore: bump version to 1.0.0` — update `VERSION`
- [x] `docs: update README and CHANGELOG for v1.0.0` — update badge + CHANGELOG
- [ ] Merge to `main`: `git checkout main && git merge develop`
- [ ] Push both: `git push origin develop main`
